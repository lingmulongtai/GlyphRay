# GlyphRay Website

Static GitHub Pages download site for GlyphRay.

## Local Preview

Open `index.html` directly in a browser. The page has no build step and no backend dependency.

## Deployment

The `pages.yml` workflow uploads this directory as a GitHub Pages artifact. Enable GitHub Pages in the repository settings and choose GitHub Actions as the source.
